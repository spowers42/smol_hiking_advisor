## Agent skills

### Issue tracker

Issues are tracked on GitHub Issues at `spowers42/smol_hiking_advisor`. External PRs are not a triage surface. See `docs/agents/issue-tracker.md`.

### Triage labels

Five canonical labels mapping to `needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context layout. See `docs/agents/domain.md`.

## Project skills

### prepare-pr

Create a branch, fix lint, run tests, and open a PR. See `.opencode/skills/prepare-pr/SKILL.md`.
