# LLM backend selection (priority order):
#   1. HF_ENDPOINT_URL  → Hugging Face inference endpoint
#   2. USE_LLAMACPP     → llama.cpp server (OpenAI-compatible API)
#   3. (default)        → Hugging Face transformers local model
#
# llama.cpp server settings (when USE_LLAMACPP=true):
#   LLAMACPP_HOST=localhost
#   LLAMACPP_PORT=8080
#   LLAMACPP_MODEL_NAME=  model alias from models.ini (optional, default: "default")
#
# Start the server with multi-model support:
#   llama-server --models-preset models.ini --host 127.0.0.1 --port 8080

# HF_ENDPOINT_URL=https://your-endpoint.hf.space
# USE_LLAMACPP=true
# LLAMACPP_HOST=localhost
# LLAMACPP_PORT=8080
# LLAMACPP_MODEL_NAME=llama-3.1-8b
# HF_MODEL_NAME=meta-llama/Llama-3.2-3B-Instruct

# Mt Washington Observatory MCP server (optional overrides)
# MCP_WEATHER_URL=https://mt-washington-weather.fastmcp.app/mcp
# MCP_WEATHER_API_KEY=your_fastmcp_bearer_token
DEEPEVAL_TELEMETRY_OPT_OUT=1